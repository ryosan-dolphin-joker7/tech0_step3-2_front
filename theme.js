// src/app/theme.js
import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#e66a63", // ボタンのプライマリカラーを赤に設定
    },
  },
});

export default theme;
